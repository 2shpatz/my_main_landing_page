name = "ohad-shpindel"
compatibility_date = "2026-08-01"

# Static-assets-only Worker: no `main` entrypoint, so every request is served
# straight from ./public. `_headers` in that directory is applied by Cloudflare.
[assets]
directory = "./public"
